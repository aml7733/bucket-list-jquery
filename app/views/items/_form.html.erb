<%= form_for(item) do |f| %>
  <% if item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %><br>
  </div><div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %><br>
  </div><div class="field">
    <%= f.label :price %><br>
    <%= f.text_field :price %><br>
  </div><div class="field">
    <%= f.label :days_cost %><br>
    <%= f.text_field :days_cost %><br>
  </div><div class="field">
    Add or Delete Your Buckets:
    <%= f.collection_check_boxes :bucket_ids, current_user.buckets, :id, :name  %> <br>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
